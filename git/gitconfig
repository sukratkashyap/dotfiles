[core]
	pager = less -FX
[alias]
    a = add
    aa = add .
    ch = checkout
    br = branch
    cm = commit
    cma = commit --amend
    cp = cherry-pick
    d = diff
    st = status
    stv = status -v
    rs = reset
    rsh = reset --soft HEAD~1
    rhh = reset --hard HEAD~1
    p = pull
    lgs = log --pretty --oneline
[push]
    default = current
[pull]
    ff = only
    rebase = true
[credential]
    helper = store
[user]
    name = Sukrat Kashyap
